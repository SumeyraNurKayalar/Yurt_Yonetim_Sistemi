<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.text.Text?>

<VBox alignment="TOP_CENTER" spacing="15.0"
      xmlns="http://javafx.com/javafx/21"
      xmlns:fx="http://javafx.com/fxml/1"
      fx:controller="org.example.controller.StaffController">

    <padding>
        <Insets bottom="20.0" left="20.0" right="20.0" top="20.0" />
    </padding>

    <Text text="Personel Yönetim Ekranı - İzinler"
          style="-fx-font-size: 20px; -fx-font-weight: bold; -fx-fill: #17a2b8;" />

    <GridPane hgap="10" vgap="10" maxWidth="700">
        <columnConstraints>
            <ColumnConstraints percentWidth="25" />
            <ColumnConstraints percentWidth="50" />
            <ColumnConstraints percentWidth="25" />
        </columnConstraints>

        <TextField fx:id="searchTextField" promptText="Arama metnini giriniz..." GridPane.columnIndex="1" GridPane.rowIndex="1" />

        <Button text="🔍 Öğrenci Ara"
                onAction="#handleSearch"
                style="-fx-background-color: #28a745; -fx-text-fill: white;"
                GridPane.columnIndex="2" GridPane.rowIndex="1" />

        <Label text="İzinli Tarih Aralığı:" GridPane.columnIndex="0" GridPane.rowIndex="2" />
        <HBox spacing="5" GridPane.columnIndex="1" GridPane.rowIndex="2">
            <DatePicker fx:id="filterStartDatePicker" promptText="Başlangıç" />
            <DatePicker fx:id="filterEndDatePicker" promptText="Bitiş" />
            <Button text="Filtrele" onAction="#handleFilterByDate" />
        </HBox>

        <Label fx:id="searchStatusLabel" text="" style="-fx-text-fill: blue;" GridPane.columnIndex="0" GridPane.rowIndex="3" GridPane.columnSpan="3" />
    </GridPane>

    <Separator />

    <TableView fx:id="permissionTable" VBox.vgrow="ALWAYS">
        <columns>
            <TableColumn fx:id="studentNameCol" text="Öğrenci Adı" prefWidth="120" />
            <TableColumn fx:id="studentRoomCol" text="Oda No" prefWidth="70" />
            <TableColumn fx:id="startDateCol" text="Başlangıç" prefWidth="90" />
            <TableColumn fx:id="endDateCol" text="Bitiş" prefWidth="90" />
            <TableColumn fx:id="reasonCol" text="Neden" prefWidth="150" />
            <TableColumn fx:id="statusCol" text="Durum" prefWidth="100" />
            <TableColumn fx:id="actionsCol" text="İşlemler" prefWidth="160" />
        </columns>
    </TableView>

</VBox>
